# Diagnostic headless benchmark archive

This is preserved raw evidence moved out of the canonical P1 path on 2026-09-20.
It is invalid for acceptance because its manifest has `headless: true` and
`HeadlessChrome` user-agent values, even though the GPU probe reported hardware.
Do not promote or cite this archive as headed acceptance evidence.
