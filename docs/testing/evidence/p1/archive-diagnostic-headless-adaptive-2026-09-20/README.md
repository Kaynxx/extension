# Diagnostic headless adaptive archive

This is preserved raw evidence moved out of the canonical P1 path on 2026-09-20.
It is invalid for acceptance because its manifest has `headless: true` and a
`HeadlessChrome` user-agent. Physical GPU status does not override that policy.
